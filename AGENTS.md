# AGENTS.md

## Cursor Cloud specific instructions

This repository ("Coastal_Fishing_Forecast") is currently an empty project scaffold. As of the initial commit, it contains only a `README.md` describing intent: "On shore fishing argo and web app."

### Current State
- **No application code, dependencies, build system, or tests exist yet.**
- No `package.json`, `requirements.txt`, `Dockerfile`, `Makefile`, or any other configuration files are present.
- The runtime environment has Node.js v22 and Python 3.12 available.

### For Future Agents
- Once application code is added, update this file with instructions on how to lint, test, build, and run the project.
- Check the README and any new documentation for setup instructions as the project evolves.
- The update script is currently a no-op (`echo`) since there are no dependencies to install.
